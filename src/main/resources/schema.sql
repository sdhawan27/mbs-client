create table user_info(
                                username varchar(25),
                                password varchar(25),
                                role varchar(5),
                                PRIMARY KEY (username));








